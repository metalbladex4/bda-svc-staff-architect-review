# Prompt Version Log

Use one row per experiment round. Change only one prompt surface at a time
unless a deliberate multi-surface reset is explicitly documented.

| Version | Date | Prompt Surface | Parent | Change Summary | Intended Effect | Eval Tracks Run | Result | Decision |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `v000` | `2026-04-15` | baseline | none | Fresh snapshot from `main:src/bda_svc/pipeline/config.yaml` after the upstream sync to `28e863b`; this branch-aware line starts from the clean mirrored runtime, including the new detect-objects safeguard against all-zero non-null boxes. | Establish the new active branch-aware baseline before carrying any further grounding work forward. | baseline run01 | first branch-aware baseline run recorded: `DESTROYED`, `PROBABLE`, bbox `[51, 37, 102, 73]` | keep |
| `v001` | `2026-04-15` | detect_objects | `v000` | Replaces the fresh upstream baseline detection wording with a shorter contrastive grounding prompt that tells the model to box the visible connected man-made body only and avoid smoke, flame column, terrain, or the smallest burning patch. | Test whether the old short-contrastive grounding idea still improves the bbox on the clean `28e863b` baseline without changing assessment or summary prompts. | `v001` run01 | produced bbox `[46, 46, 123, 92]`, kept `DESTROYED`, but escalated to `CONFIRMED` and reintroduced subtype/context drift (`locomotive`, `railway track`) | detection improvement signal; not an overall winner |
| `v002` | `2026-04-15` | assess_damage | `v001` | Keeps the stronger `v001` detection prompt intact and replaces only the assessment wording with a short, example-anchored, generic-target formulation that discourages subtype wording and pushes sustained burn cases toward `DESTROYED` + `PROBABLE` instead of `CONFIRMED`. | Test whether we can keep the stronger `v001` bbox while pulling confidence down and removing subtype drift. | `v002` run01 | preserved bbox `[46, 46, 123, 92]`, pulled confidence back to `PROBABLE`, removed target-level subtype drift, but overcorrected damage to `DAMAGED`; summary still overreaches (`track`, `complete loss`) | partial reuse only |
| `v003` | `2026-04-15` | assess_damage | `v002` | Strengthens the `v002` assessment wording with an explicit anti-downgrade rule and stronger `DESTROYED` + `PROBABLE` example for sustained-burn cases while preserving the anti-subtype and anti-overconfidence rules. | Recover `DESTROYED` without losing the stronger `v001` bbox, `PROBABLE`, or the cleaner target-level logic. | `v003` run01, run02 | preserved bbox `[46, 46, 123, 92]`, recovered `DESTROYED`, kept `PROBABLE`, kept subtype drift out of target-level logic, and repeated exactly on run02 aside from routine metadata fields; summary still overreaches (`dirt track`, `complete loss`) | confirmed working leader on the branch-aware line |
| `v004` | `2026-04-15` | summarize_scene | `v003` | Keeps the confirmed `v001` detection and `v003` assessment behavior intact and rewrites only `summarize_scene` to stay generic about terrain/context and to keep functional-impact language conservative. | Remove unsupported scene-context wording and soften summary-level functional overreach without disturbing the confirmed target-level behavior. | `v004` run01, run02 | preserved bbox `[46, 46, 123, 92]`, preserved `DESTROYED` + `PROBABLE`, produced a materially cleaner summary (`open terrain`, `severe degradation or loss of capability`), and repeated exactly on run02 aside from routine metadata fields | confirmed full-stack branch-aware leader |
| `v005` | `2026-04-16` | detect_objects | `v004` | Keeps the confirmed `v003` assessment and `v004` summary prompts intact and rewrites only `detect_objects` to add explicit neighboring-target separation and more generic non-burning target guidance. | Improve grounding generalization across image types, especially multi-target building scenes, without losing the useful tank signal. | `v005` run01 | recovered two separate targets on `destroyed_building4`, stayed stable on most mixed-pack controls, but introduced a fatal `office.jpg` false positive (`buildings`, full-frame bbox `[0, 0, 1440, 1920]`) | partial reuse only; reject as winner |
| `v006` | `2026-04-16` | detect_objects | `v005` | Keeps the valid neighboring-target separation behavior from `v005` but adds an explicit doctrinal building guard so indoor scenes, cubicles, partitions, furniture, and room interiors are not treated as building targets. | Preserve the correct two-building result on `destroyed_building4` while removing the office false positive and keeping the current tank behavior stable. | `v006` run01, run02 | preserved the two-building result on `destroyed_building4`, restored `office.jpg` to `object_not_found`, held the tank pressure case steady, did not worsen the operational-tank issue, and repeated exactly across the mixed pack aside from routine metadata fields | confirmed detect-only grounding leader |
| `v007` | `2026-04-16` | assess_damage | `v006` | Keeps the confirmed `v006` detect rule and `v004` summary, but rewrites only `assess_damage` to separate muzzle flash / firing smoke from actual target-body damage and to allow intact actively firing targets to remain `NO DAMAGE`. | Recover the `operational_tank4` case without reopening detection, while preserving the destroyed and negative controls. | `v007` run02 | recovered `operational_tank4` to `NO DAMAGE` / `CONFIRMED` with the same bbox, held the rest of the mixed pack stable at the category/confidence level, but reintroduced `K-kill` wording in `destroyed_tank15` logic | strong partial win; keep direction, not ready to promote unchanged |
| `v008` | `2026-04-16` | assess_damage | `v007` | Keeps the valid `v007` firing-signature fix but tightens `assess_damage` again so `brief_supporting_logic` stays on visible evidence only and explicitly bans kill-mechanism labels such as K-kill, mission kill, mobility kill, or firepower kill. | Preserve the `operational_tank4` recovery while removing destroyed-case wording overreach and keeping the rest of the mixed pack stable. | `v008` run01, run02 | preserved `operational_tank4` at `NO DAMAGE` / `CONFIRMED`, kept destroyed and negative controls stable, removed `K-kill` wording from `destroyed_tank15` logic, and repeated exactly across the mixed pack aside from routine metadata fields | confirmed assess-only leader |
| `v009` | `2026-04-16` | unified_stack | `v006 + v008 + v004` | Packages the already validated best branch-aware combination into one explicit version file instead of leaving it only as an implicit frozen stack from the broad sweep. | Create one clear promotion candidate for tracked config work and winner staging. | generalization_sweep run02, `v009` run01 | unified `detect_objects=v006`, `assess_damage=v008`, `summarize_scene=v004`; broad sweep shows it is the strongest cross-image branch-aware candidate so far, and focused `run01` matched the frozen winner exactly on `tank_pressure`, `operational_tank4`, and `destroyed_building4` after routine metadata removal | staged winner / current best stack |
| `v010` | `2026-04-20` | detect_objects | `v009` | Adds one higher-salience adjacent-building target-body rule to the top detection `RULES` block and tightens the later building boxing sentence so separate boxes are limited to visibly separate exterior building bodies or clearly separate collapsed exterior structural remains. | Recover the `destroyed_building4` adjacent-building split without reopening the office negative or current tank controls, and test whether user-prompt weighting is a stronger lever than doctrine-only wording. | `detect_surface` run01 in `1.2`, mirrored `detect_surface` run01 in `1.3` | recovered `destroyed_building4` to one scene-central destroyed building in both branches; `destroyed_building3` still boxed the background building; `destroyed_building6` remained broad scene partitioning; `office_negative`, `operational_tank4`, and `tank_pressure` held | promising partial win / keep iterating |
| `v011` | `2026-04-20` | detect_objects | `v010` | Keeps the `v010` adjacent-building target-body rule but adds one more scene-central/background-context rule plus a background-building contrastive example to try to suppress distant or peripheral building detections. | Preserve the `destroyed_building4` one-box recovery while fixing `destroyed_building3` and `destroyed_building6`. | `detect_surface` run02 in `1.2`, mirrored `detect_surface` run02 in `1.3` | preserved the `destroyed_building4` recovery, but `destroyed_building3` still boxed the background building and `destroyed_building6` still returned three buildings; some incidental bbox drift also returned on non-targeted controls | reject / restore `v010` |
| `v012` | `2026-04-20` | detect_objects | `v010` | Keeps the `v010` target-body rule set but restructures the building guidance into a dedicated example-heavy target-selection block plus a shorter general contrastive block, testing whether example structure is a stronger lever than additional prose rules. | Preserve the `destroyed_building4` one-box recovery while using higher-salience examples to suppress the `destroyed_building3` background false positive and reduce `destroyed_building6` scene partitioning. | `detect_surface` run03 in `1.2`, mirrored `detect_surface` run03 in `1.3` | preserved the `destroyed_building4` recovery in both branches, but `destroyed_building3` still boxed the background building and `destroyed_building6` still returned three buildings; some bboxes tightened slightly, especially in `1.3`, but not enough to count as a real behavioral win | reject / restore `v010` |
| `v013` | `2026-04-20` | detect_objects | `v010` | Keeps the `v010` target-body rule set but adds an explicit top-of-prompt building priority decision order so the model must count valid building targets before boxing them, with one supporting foreground-vs-background example. | Preserve the `destroyed_building4` one-box recovery while using stronger hierarchy to suppress `destroyed_building3` and reduce `destroyed_building6`. | `detect_surface` run04 in `1.2`, mirrored `detect_surface` run04 in `1.3` | `1.2` preserved `destroyed_building4` but still boxed the `destroyed_building3` background building and still returned three buildings on `destroyed_building6`; `1.3` did remove the `destroyed_building3` background false positive, but the result was asymmetric and `destroyed_building6` still stayed unresolved in both branches | reject / restore `v010`; keep as asymmetric signal |

## Sequence Rules

- This branch-aware lab restarts the version sequence from `v000`.
- The preserved legacy lab remains historical context, not the active evidence
  chain for this branch.
- If a candidate is run, update this table immediately after the run is
  documented.
