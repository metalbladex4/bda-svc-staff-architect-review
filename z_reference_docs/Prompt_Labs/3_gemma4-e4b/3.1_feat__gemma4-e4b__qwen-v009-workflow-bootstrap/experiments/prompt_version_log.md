# Prompt Version Log

Use one row per experiment round. Change only one prompt surface at a time
unless a deliberate multi-surface reset is explicitly documented.

| Version | Date | Prompt Surface | Parent | Change Summary | Intended Effect | Eval Tracks Run | Result | Decision |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `v000` | `2026-04-17` | baseline | none | Semantic port of the active Qwen `v009` stack into the Gemma 4 E4B model line. Detection and assessment model tags now point to `gemma4:e4b`, while the prompt surfaces preserve the current Qwen `v009` doctrinal wording and summary discipline. | Establish a Gemma baseline that is directly comparable to both `origin/main` and active Qwen `v009` without opening a new workflow path. | `v000` run01 | first live Gemma sweep succeeded via a user-local Ollama `0.21.0` server on `127.0.0.1:11435`; equipment cases and the office negative control held, but `destroyed_building4` drifted badly on multi-target building grounding and severity | keep, but start Gemma-specific iteration from this read |
| `v000` | `2026-04-19` | baseline reset | `v000` run01 | No prompt text changes. Rebuilt the Gemma baseline on the current `e7a22a9` repo base after the upstream detect contract explicitly allowed empty detections, making the old `run01` evidence chain non-portable. | Re-characterize Gemma behavior on the current repo base before any prompt iteration. | `v000` run02 | `destroyed_tank15`, `operational_building7`, and `office_negative` held; `tank_pressure` collapsed to `object_not_found`, `operational_tank4` regressed to `DAMAGED`, and `destroyed_building4` remained an undercalled building failure | replace the active anchor with `run02`; pause before `v001` until the detect-contract effect is reconsidered |
| `v001` | `2026-04-19` | `detect_objects` | `v000` run02 | Tightened the no-target instruction so `{"detections": []}` is allowed only when no doctrinal target body or connected structural remains are visibly present, and explicitly told the model to detect a visible target body even when partially obscured by fire, smoke, flame, or dust. | Reverse the explicit empty-detections abstention on `tank_pressure` without reopening the `operational_tank4` regression. | `v001` run01 two-case tank probe + `v001` run02 full six-case pack | run01 recovered both tank cases; run02 held `destroyed_tank15`, `operational_building7`, and `office_negative`, recovered `tank_pressure` and `operational_tank4`, and improved `destroyed_building4` target separation while still undercalling one building’s severity | keep as the active Gemma candidate direction and iterate from `v001`, with building-severity follow-up next |
| `v002` | `2026-04-19` | `assess_damage` | `v001` | Added building-specific severity guidance so collapsed upper stories, major rubble fields, and loss of most visible structural mass push building assessments upward toward `SEVERE DAMAGE` or `DESTROYED` instead of anchoring on surviving lower sections or facade remnants. | Improve `destroyed_building4` building-severity calibration while preserving the recovered tank cases and intact-building control. | `v002` run01 focused guard set + `v002` run02 full six-case pack | run01 improved `destroyed_building4` severity while guards held; run02 preserved all recovered equipment/control behavior, the office negative, and the building improvement across the full inherited pack | replace `v001` as the active Gemma direction and iterate from `v002` |
