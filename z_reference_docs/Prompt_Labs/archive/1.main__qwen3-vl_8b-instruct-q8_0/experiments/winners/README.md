# Winner Snapshots

Copy accepted prompt versions here after they:

- pass the fixed eval track for the prompt surface
- preserve schema reliability
- survive manual doctrinal review
- do not introduce regressions on the frozen regression set

Suggested naming:

- `v00X_system_winner.yaml`
- `v00X_assess_damage_winner.yaml`
- `v00X_detect_objects_winner.yaml`
- `v00X_summarize_scene_winner.yaml`
