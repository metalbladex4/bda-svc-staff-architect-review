# Cycle 001

Status: `v017d_dev_validated_primary`

This cycle reserves six v017 candidate slots. The dry-run-only pause was lifted
for one bounded continuation from `v017a_body_backed_candidate_filter` to
`v017b_single_target_box_span_self_filter`.

`v017a` remains a near miss, not a winner. The approved `v017b` wave may author
one worktree-local prompt overlay and hypothesis, then run only the v2 hinge,
changed-source sanity, updated-report smoke, and separate office-negative
abstention guard packs.

This approval does not allow dev, holdout, all-112, promotion, runtime config
adoption, structural guard implementation, source-truth mutation, MCP config
changes, hook edits, or tool installs.

## Latest Result

`v017b_single_target_box_span_self_filter` completed the approved bounded live
run. It is a near miss, not a winner.

- run directory:
  `runs/v017b/live_2026-05-03_032920Z/`
- decision packet:
  `runs/v017b/live_2026-05-03_032920Z/decision_packet.md`
- diagnosis:
  `reassessments/v017b_gate_result/v017b_failure_diagnosis.md`

The aggregate hinge checks passed, changed-source sanity passed, updated-report
smoke completed, positive `155` passed, and the separate office-negative
abstention guard passed. The old-gate blocker was case `101`: `v017b` emitted
one broad military-equipment group/scene box, `[75, 58, 1000, 547]`.

## Forward Gate Change

Case `101` is now removed from forward pass/fail evaluation use and retained as
diagnostic-only evidence. The active forward hinge manifest is:

`validation_manifests/human_report_challenge_v2_hinge_11_no101.yaml`

The old 12-case hinge manifest remains available for historical/manual
diagnostic context, but it is no longer the active gate for `v017c` through
`v017f`.

Near-miss diagnosis is still required before the next candidate, but it is not a
human approval gate inside the already approved six-candidate cycle. Continue
through `v017f` unless a hard stop triggers.

## Overnight v017c-v017f Result

The approved continuation completed. All four candidates passed the active
forward gates. The current recommendation is `v017d_visual_anchor_lock` as the
best balanced potential winner, with `v017f_compact_visual_anchor_balance` as
the recall-oriented alternate.

See `overnight_v017c_to_v017f_decision_packet.md`.

## Bounded Dev Validation

The bounded dev validation approved after the overnight cycle is complete. It
used the v2 dev split with case `101` removed from forward pass/fail evaluation:

`dev_validation/v017d_v017f_dev_no101/validation_manifests/human_report_challenge_v2_dev_55_no101.yaml`

Results on the 55-case no-101 dev split:

- `v017d_visual_anchor_lock`: `72` matches, `34` false negatives,
  `16` false positives, positive `155` passed with `2` matches.
- `v017f_compact_visual_anchor_balance`: `73` matches, `33` false negatives,
  `18` false positives, positive `155` passed with `2` matches.
- same-split `v014` adjusted baseline: `69` matches, `37` false negatives,
  `17` false positives.
- same-split `v009` adjusted baseline: `74` matches, `32` false negatives,
  `27` false positives.

Decision: `v017d` remains the primary balanced candidate because it improves on
the v014 no-101 dev baseline on matches, false negatives, and false positives.
`v017f` is the recall-oriented comparator but exceeds the v014 false-positive
ceiling.

Decision packet:

`dev_validation/v017d_v017f_dev_no101/dev_validation_decision_packet.md`
