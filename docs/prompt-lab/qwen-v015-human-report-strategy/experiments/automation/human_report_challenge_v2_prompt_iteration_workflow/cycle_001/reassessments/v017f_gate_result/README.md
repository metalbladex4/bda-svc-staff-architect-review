# v017f Gate Result

Status: `potential_winner_recall_variant_not_best_balanced`

`v017f_compact_visual_anchor_balance` passed the active forward gates and
achieved the best hinge recall of the `v017c`-through-`v017f` set. It also
introduced more false positives and slightly weaker changed-source / updated
smoke behavior than `v017d`, so it is retained as the recall-oriented
alternative rather than the balanced winner.

Case `101` remains diagnostic-only and is not part of forward pass/fail scoring.
