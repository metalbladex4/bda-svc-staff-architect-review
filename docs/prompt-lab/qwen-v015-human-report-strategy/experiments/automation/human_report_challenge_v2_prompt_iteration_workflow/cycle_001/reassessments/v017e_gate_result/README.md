# v017e Gate Result

Status: `potential_winner_but_not_best_so_far`

`v017e_footprint_aligned_anchors` passed the active forward gates, but it did
not improve on `v017d`. It is retained as learning evidence rather than the
current best candidate.

Case `101` remains diagnostic-only and is not part of forward pass/fail scoring.
