# Residual FN Inventory After pp046a Diagnostic Scoring

pp046a leaves `38` false negatives. Because visual review failed, this is a diagnostic FN inventory, not a locked-baseline inventory.

Classes: `building_or_structure_piece, dense_valid_target_missed, small_valid_target_missed, smoke_or_debris_obscured, unknown`.
