# v034 FP8 vLLM Precision Recovery Autonomous

Separate-model-line FP8 vLLM prompt-refinement tranche. The working baseline is `v020c_fp8_vllm_baseline = 180 / 39 / 32 / 71`. `v032d` is rejected learning evidence only and is not used as the base prompt.
