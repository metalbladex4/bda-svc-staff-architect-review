# v036 Lessons Learned

- The case-155 gain is a local nested same-wreck duplicate-box issue, not a general dense-fragment issue.
- v035a collapsed case 67 because its wording generalized too broadly to dense/crowded targets.
- The next prompt axis should be same-wreck duplicate-box language only, with no dense/partial/uncertain-fragment wording.
