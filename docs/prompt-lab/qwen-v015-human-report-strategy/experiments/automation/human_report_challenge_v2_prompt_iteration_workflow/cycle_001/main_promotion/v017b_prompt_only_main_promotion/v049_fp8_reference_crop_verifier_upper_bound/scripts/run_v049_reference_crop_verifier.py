#!/usr/bin/env python3
"""Run v049 reference-crop verifier upper-bound.

This uses reference-centered local crops generated by v048. The results are
diagnostic upper-bound evidence only and must not be counted as deployable
score because the crop placement is oracle/reference-derived.
"""

from __future__ import annotations

import base64
import csv
import datetime as dt
import hashlib
import json
import os
import re
import shutil
import subprocess
import sys
import time
import urllib.error
import urllib.request
from pathlib import Path
from typing import Any

from PIL import Image, ImageDraw


WORKTREE = Path("/home/williambenitez1/Capstone_worktrees/1.2_feat__qwen3-vl-8b-instruct__two-pass-refinement")
CAPSTONE = Path("/home/williambenitez1/Capstone")
PARENT = WORKTREE / "docs/prompt-lab/qwen-v015-human-report-strategy/experiments/automation/human_report_challenge_v2_prompt_iteration_workflow/cycle_001/main_promotion/v017b_prompt_only_main_promotion"
PACKAGE = PARENT / "v049_fp8_reference_crop_verifier_upper_bound"
V048 = PARENT / "v048_fp8_fn_recovery_crop_verifier_loop"
ENDPOINT = "http://127.0.0.1:8000/v1"
MODEL = "Qwen/Qwen3-VL-8B-Instruct-FP8"
IMAGE_ROOT = CAPSTONE / "z_reference_docs/Data_set_Storage/human_reports/images_with_reports"

OLD_V020C = {"matches": 186, "false_negatives": 33, "false_positives": 25, "combined_errors": 58}
PP045C = {"matches": 181, "false_negatives": 38, "false_positives": 11, "combined_errors": 49}
PP046A_DIAGNOSTIC = {"matches": 181, "false_negatives": 38, "false_positives": 0, "combined_errors": 38}

VERIFIER_SCHEMA = {
    "contains_target": "boolean",
    "target_type": "military_equipment|buildings|unknown",
    "visibility": "clear|partial|smoke_obscured|low_contrast|ambiguous|not_visible",
    "damage_or_relevance": "damaged_or_bda_relevant|intact_context|unclear",
    "confidence": "number from 0.0 to 1.0",
    "reason": "short text",
}

VERIFIER_PROMPT = """You are checking a focused crop from a battle-damage assessment image.
Expected target type: {target_type}

Decide whether the crop visibly contains the expected target type. Do not draw boxes.
Return JSON only with exactly these keys:
contains_target, target_type, visibility, damage_or_relevance, confidence, reason.

Use target_type "unknown" if unsure. Keep reason short."""


def now() -> str:
    return dt.datetime.now(dt.UTC).strftime("%Y-%m-%dT%H:%M:%SZ")


def write_json(path: Path, payload: Any) -> None:
    path.parent.mkdir(parents=True, exist_ok=True)
    path.write_text(json.dumps(payload, indent=2, ensure_ascii=True) + "\n", encoding="utf-8")


def write_text(path: Path, text: str) -> None:
    path.parent.mkdir(parents=True, exist_ok=True)
    path.write_text(text, encoding="utf-8")


def write_csv(path: Path, rows: list[dict[str, Any]], fields: list[str] | None = None) -> None:
    path.parent.mkdir(parents=True, exist_ok=True)
    if fields is None:
        fields = sorted({key for row in rows for key in row}) if rows else []
    with path.open("w", encoding="utf-8", newline="") as handle:
        writer = csv.DictWriter(handle, fieldnames=fields)
        if fields:
            writer.writeheader()
            writer.writerows(rows)


def metric_line(metrics: dict[str, Any]) -> str:
    return f"{metrics['matches']}/{metrics['false_negatives']}/{metrics['false_positives']}/{metrics['combined_errors']}"


def markdown_table(rows: list[dict[str, Any]], fields: list[str]) -> str:
    lines = ["|" + "|".join(fields) + "|", "|" + "|".join(["---"] * len(fields)) + "|"]
    for row in rows:
        lines.append("|" + "|".join(str(row.get(field, "")).replace("\n", " ") for field in fields) + "|")
    return "\n".join(lines)


def load_json(path: Path) -> Any:
    return json.loads(path.read_text(encoding="utf-8"))


def backend_models() -> tuple[bool, dict[str, Any]]:
    try:
        with urllib.request.urlopen(f"{ENDPOINT}/models", timeout=10) as response:
            payload = json.loads(response.read().decode("utf-8"))
            status = response.status
        model_ids = [item.get("id") for item in payload.get("data", [])]
        return 200 <= status < 300 and MODEL in model_ids, {"status_code": status, "payload": payload, "model_ids": model_ids}
    except Exception as exc:  # noqa: BLE001 - captured in evidence
        return False, {"error": repr(exc)}


def image_to_data_url(path: Path) -> str:
    suffix = path.suffix.lower()
    mime = "image/png" if suffix == ".png" else "image/jpeg"
    data = base64.b64encode(path.read_bytes()).decode("ascii")
    return f"data:{mime};base64,{data}"


def extract_json(text: str) -> tuple[bool, dict[str, Any] | None, str]:
    clean = text.strip()
    if clean.startswith("```"):
        clean = re.sub(r"^```(?:json)?\s*", "", clean)
        clean = re.sub(r"\s*```$", "", clean)
    try:
        return True, json.loads(clean), ""
    except json.JSONDecodeError:
        match = re.search(r"\{.*\}", clean, flags=re.DOTALL)
        if not match:
            return False, None, "no_json_object"
        try:
            return True, json.loads(match.group(0)), ""
        except json.JSONDecodeError as exc:
            return False, None, f"json_decode_error:{exc}"


def classify_result(parsed: dict[str, Any] | None, valid: bool, crop_status: str) -> str:
    if crop_status != "available":
        return "missing_crop"
    if not valid or parsed is None:
        return "runtime_invalid"
    contains = bool(parsed.get("contains_target"))
    confidence = float(parsed.get("confidence", 0.0) or 0.0)
    visibility = str(parsed.get("visibility", "ambiguous"))
    relevance = str(parsed.get("damage_or_relevance", "unclear"))
    if contains and confidence >= 0.75 and relevance == "damaged_or_bda_relevant":
        return "recoverable_by_crop_verifier_high_confidence"
    if contains and confidence >= 0.45 and relevance != "intact_context":
        return "recoverable_by_crop_verifier_low_confidence"
    if visibility in {"ambiguous", "low_contrast", "smoke_obscured"} or relevance == "unclear":
        return "ambiguous"
    return "not_visible_or_not_recoverable"


def call_verifier(crop_path: Path, target_type: str, timeout_s: int = 180) -> tuple[bool, dict[str, Any] | None, str, str, str]:
    prompt = VERIFIER_PROMPT.format(target_type=target_type)
    payload = {
        "model": MODEL,
        "messages": [
            {
                "role": "user",
                "content": [
                    {"type": "text", "text": prompt},
                    {"type": "image_url", "image_url": {"url": image_to_data_url(crop_path)}},
                ],
            }
        ],
        "temperature": 0,
        "max_tokens": 220,
    }
    request = urllib.request.Request(
        f"{ENDPOINT}/chat/completions",
        data=json.dumps(payload).encode("utf-8"),
        headers={"Content-Type": "application/json"},
        method="POST",
    )
    with urllib.request.urlopen(request, timeout=timeout_s) as response:
        response_payload = json.loads(response.read().decode("utf-8"))
    raw = response_payload["choices"][0]["message"]["content"]
    digest = hashlib.sha256(raw.encode("utf-8")).hexdigest()
    valid, parsed, error = extract_json(raw)
    return valid, parsed, raw, digest, error


def source_search_for_missing(image_filename: str) -> list[str]:
    candidates: list[str] = []
    roots = [
        CAPSTONE / "z_reference_docs",
        WORKTREE / "docs/prompt-lab",
        CAPSTONE / ".graphify_project_brain/corpus/main/z_reference_docs/Prompt_Labs",
    ]
    for root in roots:
        if not root.exists():
            continue
        for path in root.rglob(image_filename):
            if path.is_file() and path.name == image_filename:
                candidates.append(str(path))
    return sorted(set(candidates))


def maybe_generate_missing_crop(row: dict[str, Any], crop_root: Path) -> tuple[str, str, str]:
    image_filename = row["image_filename"]
    if image_filename != "40.png":
        return "missing", "", ""
    found = source_search_for_missing(image_filename)
    # Avoid using bbox/crop review artifacts from other model lines as source images.
    source_candidates = [
        p for p in found
        if "/images_with_reports/" in p or "/source" in p
    ]
    if not source_candidates:
        return "missing", "", ";".join(found[:20])
    source = Path(source_candidates[0])
    box = json.loads(row["reference_bbox"])
    image = Image.open(source).convert("RGB")
    x1, y1, x2, y2 = [float(v) for v in box]
    pad = max(80.0, 0.75 * max(x2 - x1, y2 - y1))
    crop = image.crop((max(0, int(x1 - pad)), max(0, int(y1 - pad)), min(image.width, int(x2 + pad)), min(image.height, int(y2 + pad))))
    draw = ImageDraw.Draw(crop)
    ox = max(0, int(x1 - pad))
    oy = max(0, int(y1 - pad))
    draw.rectangle([x1 - ox, y1 - oy, x2 - ox, y2 - oy], outline=(30, 220, 80), width=4)
    crop_path = crop_root / f"local_only_fn_case_{row['case_id']}_{row['reference_label']}_{row['target_type']}_resolved.jpg"
    crop_path.parent.mkdir(parents=True, exist_ok=True)
    crop.save(crop_path, quality=92)
    return "available", str(crop_path), str(source)


def build_manifest() -> tuple[list[dict[str, Any]], dict[str, Any]]:
    inventory = load_json(V048 / "residual_fn_inventory.json")
    crop_manifest = load_json(V048 / "fn_crop_manifest.json")
    crop_by_key = {
        (row["case_id"], row["reference_label"]): row
        for row in crop_manifest["rows"]
    }
    rows: list[dict[str, Any]] = []
    missing_report = {"generated_at": now(), "rows": []}
    resolved_root = PACKAGE / "review_images/local_only_resolved_reference_crops"
    for fn in inventory["rows"]:
        key = (fn["case_id"], fn["reference_label"])
        crop_row = crop_by_key.get(key, {})
        crop_path = crop_row.get("crop_path", "")
        crop_status = "available" if crop_path and Path(crop_path).exists() else "missing"
        source_path = str(IMAGE_ROOT / fn["image_filename"]) if (IMAGE_ROOT / fn["image_filename"]).exists() else ""
        alternate_search_hits = ""
        if crop_status == "missing":
            crop_status, resolved_crop, source_or_hits = maybe_generate_missing_crop(fn, resolved_root)
            if crop_status == "available":
                crop_path = resolved_crop
                source_path = source_or_hits
            else:
                alternate_search_hits = source_or_hits
                missing_report["rows"].append(
                    {
                        "case_id": fn["case_id"],
                        "image_filename": fn["image_filename"],
                        "reference_label": fn["reference_label"],
                        "reason": "source image not found under configured human-report source root",
                        "alternate_hits_sample": alternate_search_hits,
                    }
                )
        rows.append(
            {
                "case_id": fn["case_id"],
                "image_filename": fn["image_filename"],
                "reference_label": fn["reference_label"],
                "target_type": fn["target_type"],
                "reference_bbox": fn["reference_bbox"],
                "fn_class": fn["primary_failure_class"],
                "crop_path": crop_path,
                "crop_status": crop_status,
                "source_image_path": source_path,
                "reference_centered_oracle_diagnostic": True,
                "alternate_search_hits": alternate_search_hits,
            }
        )
    return rows, missing_report


def run_verifier(manifest_rows: list[dict[str, Any]]) -> list[dict[str, Any]]:
    results: list[dict[str, Any]] = []
    for idx, row in enumerate(manifest_rows, 1):
        crop_status = "available" if row["crop_status"] == "available" and Path(row["crop_path"]).exists() else "missing"
        valid = False
        parsed: dict[str, Any] | None = None
        raw = ""
        digest = ""
        error = ""
        if crop_status == "available":
            for attempt in range(1, 3):
                try:
                    valid, parsed, raw, digest, error = call_verifier(Path(row["crop_path"]), row["target_type"])
                    break
                except Exception as exc:  # noqa: BLE001 - evidence row
                    error = f"attempt_{attempt}:{repr(exc)}"
                    time.sleep(2)
        cls = classify_result(parsed, valid, crop_status)
        result = {
            **row,
            "verifier_index": idx,
            "json_valid": str(valid).lower(),
            "contains_target": "" if parsed is None else str(bool(parsed.get("contains_target"))).lower(),
            "verifier_target_type": "" if parsed is None else str(parsed.get("target_type", "")),
            "visibility": "" if parsed is None else str(parsed.get("visibility", "")),
            "damage_or_relevance": "" if parsed is None else str(parsed.get("damage_or_relevance", "")),
            "confidence": "" if parsed is None else parsed.get("confidence", ""),
            "reason": "" if parsed is None else str(parsed.get("reason", "")),
            "raw_response_hash": digest,
            "raw_response": raw,
            "runtime_error": error,
            "recoverability_class": cls,
        }
        results.append(result)
        print(f"verified {idx}/{len(manifest_rows)} case={row['case_id']} label={row['reference_label']} class={cls}", flush=True)
    return results


def aggregate(results: list[dict[str, Any]]) -> tuple[list[dict[str, Any]], dict[str, int]]:
    classes = sorted({row["fn_class"] for row in results})
    rows: list[dict[str, Any]] = []
    totals = {
        "high_conf_recoverable": 0,
        "low_conf_recoverable": 0,
        "ambiguous": 0,
        "not_recoverable": 0,
        "missing": 0,
        "runtime_invalid": 0,
    }
    for cls in classes:
        subset = [row for row in results if row["fn_class"] == cls]
        high = sum(row["recoverability_class"] == "recoverable_by_crop_verifier_high_confidence" for row in subset)
        low = sum(row["recoverability_class"] == "recoverable_by_crop_verifier_low_confidence" for row in subset)
        amb = sum(row["recoverability_class"] == "ambiguous" for row in subset)
        missing = sum(row["recoverability_class"] == "missing_crop" for row in subset)
        invalid = sum(row["recoverability_class"] == "runtime_invalid" for row in subset)
        not_rec = len(subset) - high - low - amb - missing - invalid
        totals["high_conf_recoverable"] += high
        totals["low_conf_recoverable"] += low
        totals["ambiguous"] += amb
        totals["missing"] += missing
        totals["runtime_invalid"] += invalid
        totals["not_recoverable"] += not_rec
        if cls == "building_or_structure_piece" and high + low >= max(1, len(subset) // 2):
            lever = "B"
            lever_note = "building-specific crop verifier/design"
        elif cls in {"dense_valid_target_missed", "small_valid_target_missed"} and high + low > 0:
            lever = "C"
            lever_note = "dense/small non-oracle tiling strategy"
        elif high + low + amb > 0:
            lever = "A"
            lever_note = "non-oracle tiling/crop pass"
        else:
            lever = "E" if amb else "D"
            lever_note = "visual review/FiftyOne or no action"
        rows.append(
            {
                "fn_class": cls,
                "total_fns": len(subset),
                "crops_available": len(subset) - missing,
                "high_confidence_recoverable": high,
                "low_confidence_recoverable": low,
                "ambiguous": amb,
                "not_recoverable": not_rec,
                "missing": missing,
                "runtime_invalid": invalid,
                "recommended_next_lever": lever,
                "recommended_next_lever_note": lever_note,
            }
        )
    return rows, totals


def choose_decision(aggregate_rows: list[dict[str, Any]], totals: dict[str, int]) -> tuple[str, str, str]:
    recoverable = totals["high_conf_recoverable"] + totals["low_conf_recoverable"]
    dense = next((row for row in aggregate_rows if row["fn_class"] == "dense_valid_target_missed"), None)
    building = next((row for row in aggregate_rows if row["fn_class"] == "building_or_structure_piece"), None)
    if totals["runtime_invalid"] > 5:
        return "E", "Verifier runtime produced too many invalid results.", "Fix runtime before interpreting crop recoverability."
    if recoverable >= 15:
        return "A", "Many FNs are recognizable in reference-centered crops.", "Run v050 non-oracle tiling/crop pass."
    if building and building["high_confidence_recoverable"] + building["low_confidence_recoverable"] >= 10:
        return "B", "Recoverability concentrates in building/structure pieces.", "Design building-specific crop verifier before broader tiling."
    if dense and dense["high_confidence_recoverable"] + dense["low_confidence_recoverable"] > 0:
        return "C", "Dense/small targets are crop-recognizable.", "Run dense-row non-oracle tiling strategy."
    if totals["missing"] > 5 or totals["runtime_invalid"] > 0:
        return "E", "Missing/runtime gaps limit interpretability.", "Fix artifacts/runtime."
    return "D", "Crop verifier shows low recoverability.", "Stop FN recovery or switch to manual/FiftyOne review."


def non_oracle_strategy(decision: str, aggregate_rows: list[dict[str, Any]]) -> dict[str, Any]:
    return {
        "generated_at": now(),
        "reference_centered_results_are_not_deployable": True,
        "decision": decision,
        "options": [
            {
                "option": "fixed_tile_grid",
                "crop_generation_without_ground_truth": "2x2 or 3x3 overlapping grid over full image; no reference boxes.",
                "expected_fn_classes_recovered": ["dense_valid_target_missed", "small_valid_target_missed", "smoke_or_debris_obscured"],
                "fp_risk": "medium",
                "merge_strategy": "map crop detections to image coordinates, require verifier BDA relevance, then run existing FP postprocessors.",
                "scoring_strategy": "full all-current/no101 raw plus postprocessed scoring; reference boxes used only after inference for eval.",
                "micro_pack_gate": [12, 14, 16, 42, 66, 67, 77, 84, 88, 90, 97, 100, 103, 110, 155, 166, 172],
                "stop_criteria": "any FP increase on dense/control/office-negative or JSON instability.",
            },
            {
                "option": "existing_detection_anchor_crops",
                "crop_generation_without_ground_truth": "crop around existing detections plus surrounding context to find nearby missed targets.",
                "expected_fn_classes_recovered": ["adjacent_target_confusion", "dense_valid_target_missed"],
                "fp_risk": "medium_high",
                "merge_strategy": "only add candidates not contained by anchors and not duplicate-like; require high verifier confidence.",
                "scoring_strategy": "compare candidate additions against pp045c baseline, with pp046a unsafe cases monitored.",
                "micro_pack_gate": [66, 67, 84, 100, 110, 155, 166],
                "stop_criteria": "reintroduces pp046a unsafe removals or adds local artifacts.",
            },
            {
                "option": "high_uncertainty_zone_crops",
                "crop_generation_without_ground_truth": "crop low-confidence/edge/dense regions from prediction geometry and image tiling heuristics.",
                "expected_fn_classes_recovered": ["edge_or_boundary_target", "small_valid_target_missed"],
                "fp_risk": "high_without_verifier",
                "merge_strategy": "candidate must pass crop verifier and not overlap intact context.",
                "scoring_strategy": "offline experiment only; no product runtime mutation.",
                "micro_pack_gate": [14, 42, 67, 84, 110, 141, 146],
                "stop_criteria": "candidate generation depends on reference boxes or adds context FPs.",
            },
        ],
        "recommended_v050": "Implement fixed non-oracle tiling/crop pass first if recoverability is broad; otherwise specialize by the strongest recoverable class.",
        "recoverability_inputs": aggregate_rows,
    }


def main() -> None:
    generated = now()
    for subdir in ["scripts", "tables", "review_images", "verifier_designs", "runs"]:
        (PACKAGE / subdir).mkdir(parents=True, exist_ok=True)

    backend_ok, backend_payload = backend_models()
    backend_preflight = {
        "generated_at": generated,
        "backend_available": backend_ok,
        "endpoint": ENDPOINT,
        "model": MODEL,
        "models_response": backend_payload,
        "launch_command": "uvx --from vllm python -m vllm.entrypoints.openai.api_server --host 127.0.0.1 --port 8000 --model Qwen/Qwen3-VL-8B-Instruct-FP8 --served-model-name Qwen/Qwen3-VL-8B-Instruct-FP8 --trust-remote-code --dtype bfloat16 --max-model-len 4096 --limit-mm-per-prompt '{\"image\":2}' --gpu-memory-utilization 0.90 --enforce-eager --seed 42 --generation-config vllm --mm-processor-kwargs '{\"min_pixels\":784,\"max_pixels\":1003520}'",
        "log_path": str(PACKAGE / "runs/backend_logs/vllm_fp8_foreground_20260510T002600Z.log"),
    }
    write_json(PACKAGE / "backend_preflight.json", backend_preflight)
    if not backend_ok:
        raise RuntimeError(f"Backend unavailable: {backend_payload}")

    manifest_rows, missing_report = build_manifest()
    available = sum(row["crop_status"] == "available" and Path(row["crop_path"]).exists() for row in manifest_rows)
    missing = len(manifest_rows) - available
    print(f"""=== V049 STATUS ===
phase: preflight
backend: available
fn_crops_available: {available}
fn_crops_missing: {missing}
verifier_runs: 0
high_conf_recoverable: 0
low_conf_recoverable: 0
ambiguous: 0
not_recoverable: 0
decision: E
main_lesson: Backend is available and reference-crop manifest can be built from v048 local crops.
next_action: Run compact crop verifier over available crops.
===================""")

    results = run_verifier(manifest_rows)
    aggregate_rows, totals = aggregate(results)
    decision, main_lesson, next_action = choose_decision(aggregate_rows, totals)
    strategy = non_oracle_strategy(decision, aggregate_rows)
    runs = len([row for row in results if row["crop_status"] == "available"])

    source_manifest = {
        "generated_at": generated,
        "package": str(PACKAGE),
        "source_artifacts_read": [
            str(V048 / "final_recommendation.md"),
            str(V048 / "residual_fn_inventory.md"),
            str(V048 / "residual_fn_inventory.json"),
            str(V048 / "residual_fn_taxonomy.csv"),
            str(V048 / "fn_crop_manifest.csv"),
            str(V048 / "verifier_design_plan.md"),
            str(PARENT / "v047_fp8_pp046a_visual_verification_and_fn_recovery/final_recommendation.md"),
            str(PARENT / "v046_fp8_pp045_visual_verification_and_continuation/final_recommendation.md"),
            str(CAPSTONE / "z_reference_docs/GPT-Pro_collab/bda_svc_qwen_prompt_engineering_deep_research_bundle.md"),
            str(CAPSTONE / "z_reference_docs/GPT-Pro_collab/CODEX_QWEN_PROMPT_ENGINEERING_DIRECTION_2026-05-06.md"),
            str(CAPSTONE / "z_reference_docs/Prompting"),
        ],
        "metrics_confirmed": {
            "old_v020c": OLD_V020C,
            "pp045c_locked_baseline": PP045C,
            "pp046a_diagnostic_only": PP046A_DIAGNOSTIC,
            "v024o": "partial_unscored_forbidden",
        },
        "hard_boundaries": [
            "experiment-only",
            "reference-centered crops are oracle diagnostic only",
            "no product runtime mutation",
            "no prompt candidates",
            "no product score from crop results",
            "no raw image push",
        ],
    }

    prompt_doc = {
        "generated_at": generated,
        "prompt_template": VERIFIER_PROMPT,
        "schema": VERIFIER_SCHEMA,
        "runtime_case_ids_in_prompt": False,
        "reference_labels_in_prompt": False,
        "expected_target_type_in_prompt": True,
        "asks_for_boxes": False,
    }
    recoverability = {
        "generated_at": generated,
        "totals": totals,
        "rows": aggregate_rows,
        "decision": decision,
    }
    final = {
        "generated_at": generated,
        "backend_ran": True,
        "fn_crops_available": available,
        "fn_crops_missing": missing,
        "verifier_runs": runs,
        "high_confidence_recoverable": totals["high_conf_recoverable"],
        "low_confidence_recoverable": totals["low_conf_recoverable"],
        "ambiguous": totals["ambiguous"],
        "not_recoverable": totals["not_recoverable"],
        "runtime_invalid": totals["runtime_invalid"],
        "missing": totals["missing"],
        "dominant_recoverable_fn_classes": [
            row["fn_class"]
            for row in aggregate_rows
            if row["high_confidence_recoverable"] + row["low_confidence_recoverable"] > 0
        ],
        "decision": decision,
        "next_work": next_action,
        "reference_centered_results_are_not_deployable": True,
        "hard_boundaries_preserved": True,
    }

    write_json(PACKAGE / "source_manifest.json", source_manifest)
    write_json(PACKAGE / "crop_verifier_manifest.json", {"generated_at": generated, "rows": manifest_rows, "crops_available": available, "crops_missing": missing})
    write_text(PACKAGE / "crop_verifier_manifest.md", f"""# Crop Verifier Manifest

Reference-centered crop rows: `{len(manifest_rows)}`.

Crops available: `{available}`.

Crops missing: `{missing}`.

All crop placements are oracle/reference-centered and diagnostic only.
""")
    write_json(PACKAGE / "crop_verifier_prompt.json", prompt_doc)
    write_text(PACKAGE / "crop_verifier_prompt.md", f"""# Crop Verifier Prompt

This compact verifier prompt was used for cropped images only. It does not ask for boxes and does not include case IDs.

```text
{VERIFIER_PROMPT}
```

## Schema

```json
{json.dumps(VERIFIER_SCHEMA, indent=2)}
```
""")
    results_for_csv = [{k: v for k, v in row.items() if k != "raw_response"} for row in results]
    write_json(PACKAGE / "crop_verifier_results.json", {"generated_at": generated, "rows": results, "totals": totals})
    write_csv(PACKAGE / "crop_verifier_results.csv", results_for_csv)
    write_text(PACKAGE / "crop_verifier_results.md", f"""# Crop Verifier Results

Verifier runs: `{runs}`.

High-confidence recoverable: `{totals['high_conf_recoverable']}`.

Low-confidence recoverable: `{totals['low_conf_recoverable']}`.

Ambiguous: `{totals['ambiguous']}`.

Not recoverable: `{totals['not_recoverable']}`.

Missing crop: `{totals['missing']}`.

Runtime invalid: `{totals['runtime_invalid']}`.

These are reference-centered upper-bound results, not deployable score.
""")
    write_json(PACKAGE / "recoverability_by_class.json", recoverability)
    write_text(PACKAGE / "recoverability_by_class.md", f"""# Recoverability By Class

{markdown_table(aggregate_rows, ['fn_class', 'total_fns', 'crops_available', 'high_confidence_recoverable', 'low_confidence_recoverable', 'ambiguous', 'not_recoverable', 'missing', 'runtime_invalid', 'recommended_next_lever', 'recommended_next_lever_note'])}
""")
    write_json(PACKAGE / "missing_crop_report.json", missing_report)
    write_text(PACKAGE / "missing_crop_report.md", f"""# Missing Crop Report

Missing crops: `{missing}`.

{markdown_table(missing_report['rows'], ['case_id', 'image_filename', 'reference_label', 'reason']) if missing_report['rows'] else 'No missing crops.'}
""")
    write_json(PACKAGE / "non_oracle_tiling_strategy.json", strategy)
    write_text(PACKAGE / "non_oracle_tiling_strategy.md", f"""# Non-Oracle Tiling Strategy

Reference-centered crops cannot be deployed or counted as product score. v050 should generate crops without ground truth.

Decision: `{decision}`.

Recommended next action: {next_action}

## Options

{markdown_table(strategy['options'], ['option', 'crop_generation_without_ground_truth', 'expected_fn_classes_recovered', 'fp_risk', 'merge_strategy', 'stop_criteria'])}
""")
    write_text(PACKAGE / "README.md", "# v049 FP8 Reference-Crop Verifier Upper Bound\n\nExperiment-only diagnostic upper-bound test using reference-centered crops from v048. Do not treat results as deployable score.\n")
    write_json(PACKAGE / "recovery_log.json", {"generated_at": generated, "events": ["backend preflight passed", "manifest built", "reference-crop verifier run", "recoverability aggregated", "non-oracle tiling strategy written"]})
    write_text(PACKAGE / "recovery_log.md", "# Recovery Log\n\n- Backend preflight passed.\n- Built crop manifest from v048.\n- Ran crop verifier over available reference-centered crops.\n- Aggregated recoverability by FN class.\n- Wrote non-oracle v050 tiling/crop strategy.\n")
    write_text(PACKAGE / "lessons_learned.md", f"""# Lessons Learned

- Reference-centered crop verification is useful as an upper bound, but it is not deployable.
- pp045c remains the locked baseline at `{metric_line(PP045C)}`.
- pp046a remains diagnostic only at `{metric_line(PP046A_DIAGNOSTIC)}`.
- Decision `{decision}`: {main_lesson}
""")
    write_text(PACKAGE / "strategy_state.md", f"""# Strategy State

Locked baseline: `pp045c = {metric_line(PP045C)}`.

Diagnostic pp046a: `{metric_line(PP046A_DIAGNOSTIC)}`, not locked.

Reference-crop verifier runs: `{runs}`.

High-confidence recoverable: `{totals['high_conf_recoverable']}`.

Decision: `{decision}`.

Next action: {next_action}
""")
    write_json(PACKAGE / "final_recommendation.json", final)
    write_text(PACKAGE / "final_recommendation.md", f"""# v049 Final Recommendation

Generated: `{generated}`

Backend ran: `true`.

FN crops available/missing: `{available}` / `{missing}`.

Verifier runs: `{runs}`.

High-confidence recoverable: `{totals['high_conf_recoverable']}`.

Low-confidence recoverable: `{totals['low_conf_recoverable']}`.

Ambiguous: `{totals['ambiguous']}`.

Not recoverable: `{totals['not_recoverable']}`.

Decision: `{decision}`.

Main lesson: {main_lesson}

Next action: {next_action}

## Recoverability By Class

{markdown_table(aggregate_rows, ['fn_class', 'total_fns', 'crops_available', 'high_confidence_recoverable', 'low_confidence_recoverable', 'ambiguous', 'not_recoverable', 'missing', 'runtime_invalid', 'recommended_next_lever_note'])}

## Boundary

These are reference-centered crop results. They are diagnostic upper-bound evidence only, not deployable score, not product runtime, and not a replacement for old/product v020c.
""")
    write_text(PACKAGE / "tables/.gitkeep", "placeholder for generated textual tables\n")
    write_text(PACKAGE / "review_images/.gitkeep", "placeholder only; local crop images are not pushed\n")
    write_text(PACKAGE / "verifier_designs/crop_verifier_prompt.md", (PACKAGE / "crop_verifier_prompt.md").read_text(encoding="utf-8"))
    write_json(PACKAGE / "verifier_designs/crop_verifier_prompt.json", prompt_doc)

    print(f"""=== V049 STATUS ===
phase: crop_verifier_run
backend: available
fn_crops_available: {available}
fn_crops_missing: {missing}
verifier_runs: {runs}
high_conf_recoverable: {totals['high_conf_recoverable']}
low_conf_recoverable: {totals['low_conf_recoverable']}
ambiguous: {totals['ambiguous']}
not_recoverable: {totals['not_recoverable']}
decision: {decision}
main_lesson: {main_lesson}
next_action: {next_action}
===================""")
    print(f"""=== V049 STATUS ===
phase: recoverability_summary
backend: available
fn_crops_available: {available}
fn_crops_missing: {missing}
verifier_runs: {runs}
high_conf_recoverable: {totals['high_conf_recoverable']}
low_conf_recoverable: {totals['low_conf_recoverable']}
ambiguous: {totals['ambiguous']}
not_recoverable: {totals['not_recoverable']}
decision: {decision}
main_lesson: {main_lesson}
next_action: {next_action}
===================""")
    print(f"""=== V049 STATUS ===
phase: tiling_strategy
backend: n/a
fn_crops_available: {available}
fn_crops_missing: {missing}
verifier_runs: {runs}
high_conf_recoverable: {totals['high_conf_recoverable']}
low_conf_recoverable: {totals['low_conf_recoverable']}
ambiguous: {totals['ambiguous']}
not_recoverable: {totals['not_recoverable']}
decision: {decision}
main_lesson: Reference-centered crops cannot be deployed, so v050 must use non-oracle crop generation.
next_action: {next_action}
===================""")
    print(f"""=== V049 STATUS ===
phase: final_decision
backend: available
fn_crops_available: {available}
fn_crops_missing: {missing}
verifier_runs: {runs}
high_conf_recoverable: {totals['high_conf_recoverable']}
low_conf_recoverable: {totals['low_conf_recoverable']}
ambiguous: {totals['ambiguous']}
not_recoverable: {totals['not_recoverable']}
decision: {decision}
main_lesson: {main_lesson}
next_action: {next_action}
===================""")


if __name__ == "__main__":
    main()
