# Scripts

No helper script is required for this scaffolding wave.

If the visual review later needs automation, add a small helper that reads the
existing `v020c` and `v024l` eval summaries, but do not run inference or score
`v024o`.
