# Overlays

No new prompt overlay is authored in this wave.

Do not add a `detect_objects` candidate here until
`../v020c_v024l_delta_review.md` names the dominant visual failure class and
fills the next-candidate gate.
