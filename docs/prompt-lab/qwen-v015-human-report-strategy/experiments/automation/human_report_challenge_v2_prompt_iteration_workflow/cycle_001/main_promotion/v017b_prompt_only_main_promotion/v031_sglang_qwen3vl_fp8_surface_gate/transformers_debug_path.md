# Transformers Debug Path

Not run in v031. vLLM backup successfully launched official FP8 and completed stability plus full-baseline gates, so Transformers remained a debug-only path rather than a scoring surface.
