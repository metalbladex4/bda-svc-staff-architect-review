# v050 FP8 Non-Oracle Tiling/Crop Recovery

Experiment-only tranche for testing whether v049 reference-crop recoverability transfers to non-oracle image-geometry and prediction-anchored crops.

This does not promote FP8, does not mutate product runtime, and does not replace old/product v020c (`186 / 33 / 25 / 58`).

Locked FP8 composite baseline: pp045c (`181 / 38 / 11 / 49`).

Diagnostic-only score not used as locked baseline: pp046a (`181 / 38 / 0 / 38`).

v024o remains partial/unscored and forbidden as scored evidence.
