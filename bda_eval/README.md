# Evaluation application to assess bda-svc

## Usage

1. **For complete usage information**:
   ```bash
   python eval.py -h
   ```

2. **Run the bda-eval application**:
   ```bash
   python eval.py -r path\to\ref\reports -p path\to\pred\reports -o path\to\eval\reports
   ```

## Project Structure
```
TBD
```